package com.dominikbilik.smartgrid.measureddata.api.dto.measurements.enums;

public enum MeasurementTypeByTime {
    PERIODICAL, // date/time FROM and TO are different
    INSTANT; // date/time FROM and TO  are same
}
